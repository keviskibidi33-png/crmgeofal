import React from 'react';

const ExampleComponent = () => (
  <div style={{ padding: '2rem', background: '#f5f6fa', borderRadius: 8 }}>
    <h2>¡Componente de ejemplo funcionando!</h2>
    <p>Puedes empezar a crear tus propios componentes aquí.</p>
  </div>
);

export default ExampleComponent;
